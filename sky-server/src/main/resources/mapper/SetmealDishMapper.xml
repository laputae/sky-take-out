<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">
    <select id="getSetMealIdsByDishIds" resultType="java.lang.Long">
        select setmeal_id from setmeal_dish where dish_id in
        <foreach collection="dishIds" item="dishId" separator="," open="(" close=")">
            #{dishIds}
        </foreach>
    </select>
    <select id="list" parameterType="Setmeal" resultType="Setmeal">
        select * from setmeal
        <where>
            <if test="name != null">
                and name like concat('%',#{name},'%')
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>
    <delete id="deleteBySetmealId" parameterType="java.lang.Long">
        delete from setmeal_dish where setmeal_id = #{setmealId}
    </delete>
    <insert id="insertBatch" parameterType="java.util.List">
        insert into setmeal_dish (setmeal_id, dish_id, name, price, copies)
        values
        <foreach collection="setmealDishes" item="setmealDish" separator=",">
            (#{setmealDish.setmealId}, #{setmealDish.dishId}, #{setmealDish.name}, #{setmealDish.price},
            #{setmealDish.copies})
        </foreach>
    </insert>
    <select id="getSetmealDishById" parameterType="java.lang.Long" resultType="com.sky.entity.SetmealDish">
        select sd.* from setmeal, setmeal_dish as sd
        where setmeal.id=sd.setmeal_id and sd.setmeal_id=#{id}
    </select>
</mapper>